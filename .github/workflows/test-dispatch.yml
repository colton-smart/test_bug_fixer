name: AI Pipeline Debug (Phase 4)

on:
  repository_dispatch:
    types:
      - jira-bug-created
      - jira-bug-updated

jobs:
  build-branch:
    runs-on: ubuntu-latest

    env:
      GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}

    permissions:
      contents: write
      pull-requests: write

    steps:
      # ─────────────────────────────────────────────
      # 1. Extract Jira payload (source of truth)
      # ─────────────────────────────────────────────
      - name: Extract variables
        run: |
          echo "JIRA_KEY=${{ github.event.client_payload.jira_key }}" >> $GITHUB_ENV
          echo "SUMMARY=${{ github.event.client_payload.summary }}" >> $GITHUB_ENV
          echo "BRANCH=ai-fix/${{ github.event.client_payload.jira_key }}" >> $GITHUB_ENV

      # ─────────────────────────────────────────────
      # 2. Checkout repo
      # ─────────────────────────────────────────────
      - name: Checkout repo
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      # ─────────────────────────────────────────────
      # 3. Create or reuse branch (idempotent)
      # ─────────────────────────────────────────────
      - name: Create or reuse branch
        run: |
          git fetch origin

          if git ls-remote --exit-code --heads origin "$BRANCH"; then
            echo "Branch exists remotely. Checking out."
            git checkout -b "$BRANCH" origin/"$BRANCH"
          else
            echo "Creating new branch."
            git checkout -b "$BRANCH"
          fi

      # ─────────────────────────────────────────────
      # 4. Simulate change (placeholder for AI step)
      # ─────────────────────────────────────────────
      - name: Simulate change
        run: |
          echo "Fix placeholder for ${JIRA_KEY}" > ai-fix.txt

      # ─────────────────────────────────────────────
      # 5. Commit changes
      # ─────────────────────────────────────────────
      - name: Commit changes
        run: |
          git config user.name "ai-bot"
          git config user.email "ai-bot@test.local"

          git add .
          git commit -m "fix(${JIRA_KEY}): ${SUMMARY}" || echo "No changes to commit"

      # ─────────────────────────────────────────────
      # 6. Push branch
      # ─────────────────────────────────────────────
      - name: Push branch
        run: |
          git push origin "$BRANCH"

      # ─────────────────────────────────────────────
      # 7. Detect existing PR (safe + deterministic)
      # ─────────────────────────────────────────────
      - name: Detect existing PR
        run: |
          set -e

          PR_JSON=$(gh pr list --search "$BRANCH in:head" --json number,url)

          PR_COUNT=$(echo "$PR_JSON" | jq length)

          if [ "$PR_COUNT" -gt 0 ]; then
            echo "PR_EXISTS=true" >> $GITHUB_ENV
            echo "PR_NUMBER=$(echo "$PR_JSON" | jq -r '.[0].number')" >> $GITHUB_ENV
            echo "PR_URL=$(echo "$PR_JSON" | jq -r '.[0].url')" >> $GITHUB_ENV
          else
            echo "PR_EXISTS=false" >> $GITHUB_ENV
          fi

      # ─────────────────────────────────────────────
      # 8. Create or update PR (NO label deletion)
      # ─────────────────────────────────────────────
      - name: Create or update PR
        run: |
          if [ "$PR_EXISTS" = "true" ]; then
            echo "Updating existing PR: $PR_URL"

            gh pr edit "$PR_NUMBER" \
              --add-label "ai-pipeline:updated"
          else
            echo "Creating new PR"

            gh pr create \
              --base main \
              --head "$BRANCH" \
              --title "[AI Fix] $JIRA_KEY - $SUMMARY" \
              --body "Initial AI pipeline PR for $JIRA_KEY" \
              --label "ai-fix,ai-pipeline:created"
          fi

      # ─────────────────────────────────────────────
      # 9. Audit trail comment
      # ─────────────────────────────────────────────
      - name: Append PR audit note
        run: |
          if [ "$PR_EXISTS" = "true" ]; then
            gh pr comment "$PR_NUMBER" \
              --body "🔄 Jira update received for ${JIRA_KEY} at $(date -u)."
          fi
